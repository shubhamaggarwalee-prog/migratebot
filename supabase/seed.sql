-- ============================================================
-- MigrateBot Seed Data (for local development only)
-- DO NOT run in production
-- ============================================================

-- Insert a test migration (replace user_id with a real Supabase auth user UUID)
-- INSERT INTO migrations (user_id, repourl, reponame, source_platform, tier, status)
-- VALUES (
--   'your-user-uuid-here',
--   'https://github.com/vercel/next.js',
--   'next.js',
--   'github',
--   'standard',
--   'pending'
-- );

SELECT 'Seed file loaded. Uncomment inserts above to add test data.' AS message;
